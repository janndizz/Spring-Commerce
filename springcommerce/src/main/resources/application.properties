spring.application.name=spring-commerce
spring.datasource.url=jdbc:mysql://localhost:3306/springcommerce_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Letuannhan11062004.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Thymeleaf Configuration (optional, for caching)
spring.thymeleaf.cache=false

# Fallback to H2 if MySQL is not available
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=5

# Disable SQL script initialization
spring.sql.init.mode=never

server.servlet.session.timeout=72000
